{
  "name": "udtalk-obs-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "UDトークのWeb公開会話をOBSテキストへ転送する最小ブリッジ",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/src/index.js",
    "test": "npm run build && node --test dist/test/*.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^7.0.2"
  }
}
